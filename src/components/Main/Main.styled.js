import styled from 'styled-components';

export const MainContainer = styled.main`
    padding: 0 calc(100vw/12);
`

export const MainCardsContainer = styled.section`
    width: calc((100vw/12)*6); 
    padding: 0;
    display: flex;
    justify-content: space-between;
    align-items: center;
`